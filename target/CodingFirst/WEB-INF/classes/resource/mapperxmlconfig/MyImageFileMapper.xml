<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.colin.mapper.MyImageFileMapper">
    <update id="updateFileById">
        update user set user_picture = #{param1} where user_id = #{param2}
    </update>
    <update id="uploadFileOfCompanyById">
         update company set company_picture = #{param1} where company_id = #{param2}
    </update>
    <select id="selectImageFile" resultType="java.lang.String">
        select user_picture from user where user_id = #{id}
    </select>
    <select id="selectImageOfCompany" resultType="java.lang.String">
         select company_picture from company where company_id = #{id}
    </select>
</mapper>